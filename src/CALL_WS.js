/**
 * Symbol key that carries Websockets call info interpreted by this Redux middleware.
 *
 * @constant {symbol}
 * @access public
 * @default
 */
const CALL_WS = Symbol('Call WS');

export default CALL_WS;